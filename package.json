{
  "name": "hexlet-playwright",
  "version": "1.0.0",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "scripts": {
    "test:codegen": "playwright codegen",
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui",
    "show:report": "npx playwright show-report",
    "test:debug-logs": "set DEBUG=* && set DEBUG_FILE=logs.txt && npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^22.12.0"
  }
}
